@model IEnumerable<Graft.Models.Details>
<div id="test">
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    
  

@foreach (var item in Model) {
    <tr>
        <td>
            @item.id
        </td>
        <td>
            @item.name
        </td>
    </tr>
}

</table>
</div>